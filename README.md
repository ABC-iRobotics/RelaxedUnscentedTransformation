# RelaxedUnscentedTransformation

Install:
use the install.bat file to build and install third party and the project the arguments are the build and the install folder as
$ install ../build ../install